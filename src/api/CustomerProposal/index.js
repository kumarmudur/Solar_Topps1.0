import { API_HOST_BASE } from '../../constants/constants';

const API_HOST = `${API_HOST_BASE}/api/proposal/v1`;

const API = {
    // GET METHODS
    GET_PROPOSAL_LIST: `${API_HOST}/admin/proposal`,

   

    // POST METHODS

}

export default API;
